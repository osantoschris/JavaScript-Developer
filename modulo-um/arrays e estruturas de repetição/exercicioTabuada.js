// Crie um programa que dado um número crie e imprima a sua tabuada

const numero = 125;

for (let i = 0; i <= 10; i++) {
    const resultado = numero * i
    console.log(`${numero} x ${i} = ${resultado}`);
}
